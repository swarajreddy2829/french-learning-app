/**
 * Learner-scoped progress queries and derived progress representations.
 *
 * <p>This feature builds read models from lesson completions and quiz attempts.
 * It does not own editable progress state, and every query must derive
 * ownership from the authenticated subject.</p>
 */
package com.example.frenchlearning.progress;
