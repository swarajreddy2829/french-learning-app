/**
 * Lesson content, revision lifecycle, discovery, and completion use cases.
 *
 * <p>This feature owns lesson-domain invariants and lesson persistence.
 * Controllers expose DTOs through services and must not expose persistence
 * entities or call repositories directly.</p>
 */
package com.example.frenchlearning.lesson;
