/**
 * Small, feature-neutral API and persistence primitives shared across modules.
 *
 * <p>Only concepts with stable meaning across multiple features belong here.
 * Feature-specific rules must remain in their owning package to prevent this
 * package from becoming a general-purpose dependency hub.</p>
 */
package com.example.frenchlearning.common;
